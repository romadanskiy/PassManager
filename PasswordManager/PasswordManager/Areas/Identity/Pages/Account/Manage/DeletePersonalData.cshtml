@page
@model DeletePersonalDataModel
@{
    ViewData["Title"] = "Удаление личных данных";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<h4>@ViewData["Title"]</h4>

<div class="alert alert-warning" role="alert">
    <p>
        <strong>Удаление этих данных приведет к удалению вашей учетной записи без возможности восстановления.</strong>
    </p>
</div>

<div>
    <form id="delete-user" method="post" class="form-group">
        <div asp-validation-summary="All" class="text-danger"></div>
        @if (Model.RequirePassword)
        {
            <div class="form-group">
                <label asp-for="Input.Password"></label>
                <input asp-for="Input.Password" class="form-control" />
            </div>
        }
        <button class="btn btn-danger" type="submit">Удалить данные и закрыть мою учетную запись</button>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}